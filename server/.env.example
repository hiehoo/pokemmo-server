# Solana Configuration
SOLANA_NETWORK=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com

# Game Treasury Wallet (for rewards distribution)
# Generate a new keypair and add the secret key here
TREASURY_PRIVATE_KEY=

# Optional: Custom SPL Token Mint Address for game rewards
# Leave empty to use SOL for rewards
GAME_TOKEN_MINT=

# Reward amounts (in lamports for SOL, or token units for SPL tokens)
REWARD_PLAYER_JOIN=100000
REWARD_MAP_CHANGE=50000
REWARD_BATTLE_WIN=500000